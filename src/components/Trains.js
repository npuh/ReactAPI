import React from "react";

const Trains = props => (
  <span>
    <h3>Checkout the details!</h3>
    <div>{/* <button className="goBackButton">Go Back</button> */}</div>
  </span>
);

export default Trains;
